]
	192.168.2.58 :		root passwd --> su 	--> ketan$321
						sudo su --> ketan$123

1]	scp  -P 8623 -r detectNew* 192.168.2.58:/tmpdata/Rushikesh_27022026/detectNewApp
	root@192.168.2.58's password: 

2]	grep -irn "File Attached => path =" source/ --include="*.cpp" --include="*.h" --exclude="*.cpp_*" --exclude="*.txt" --color	

	grep → search text

	-i → ignore case

	-r → recursive search in directories

	-n → show line number

	"File Attached => path =" → text pattern to search

	source/ → directory to search in

	--include="*.cpp" → search only .cpp files

	--include="*.h" → search only .h files

	--exclude="*.cpp_*" → ignore files like file.cpp_08_01_2024

	--exclude="*.txt" → ignore .txt files

	--color → highlight matched text


3]	unlink() is a system call used to delete a file name from the filesystem. It comes from the POSIX API.

1️⃣ What unlink() Does

unlink() removes a file from a directory.

4]	grep -Rin "unlink" *.cpp *.h
	
	grep = Global Regular Expression Print

	It searches text inside files.
	
	Option	Meaning
-R	recursive search in folders
-i	ignore case
-n	show line numbers
	
	grep -Rin --include=\*.{cpp,h} "unlink" .
grep -rin --include=\*.{cpp,h} "unlink" .
Difference Between -R and -r in grep
Option	Meaning	Symbolic Links Behavior	Example Situation
-r	Recursive search in directories	Does NOT follow symbolic links	Searches only real directories
-R	Recursive search in directories	Follows symbolic links	Also searches linked directories

5]	grep -Rin "unlink" *.cpp *.h

6]	XXPRINT(1, "Rushi %s:%d %s() deletePackageList().\n", __FILE__, __LINE__, __FUNCTION__);

7] 	eScan selfprotection 
	/opt/MicroWorld/sbin/commonutil writeconfig /opt/MicroWorld/etc/customprocesslist.conf SelfProtection:Status "0"

8]	find . -type f | wc -l 
	ls -1          | wc -l      -> count no. of files in current directory
	ls -1 /path    | wc -l


9]
	:set number 

		show number of line in vim editior linux.

	:set nonumber
		remove number on vim

10] 	/opt/MicroWorld/sbin/commonutil writeconfig /opt/MicroWorld/etc/customprocesslist.conf "SelfProtection:Status" 0

	/opt/MicroWorld/sbin/commonutil writeconfig  /opt/MicroWorld/etc/GAblocker.conf "GAblocker:Status" 0

11]	vbox cli command

	start vbox using cli

	omkar@PTLD-3:/tmp$ vboxmanage startvm "Centos-7_Developement-217" -type headless

	vboxmanage list vms

	vboxmanage controlvm "Centos-7_Developement-217”  poweroff

12]  	echo $? check pervious command execute right ya wrong.
	
	o/p - >  0 	sucess
		 1	failed
		 127	wrong command
13]	ssh -o HostKeyAlgorithms=ssh-rsa root@192.168.1.226	

14] 	du -sh / du -h
	df -h
	free -h 
	lsblk

No, 1st in do while loop at start 1st getPackageStatus() has to create software.list txt file .
in 2nd time loop check any new app install/uninstall said by checkappInstallUninstalled.cpp thread then only then run getPackageStatus() and create new software.list txt file.
this is checkappInstallUninstalled.cpp c++ code 





Generating software.list
send event

Monitoring directory: /usr/share/applications/

File Created: vlc.desktop
App install/uninstall detected

Generating software.list
send event

Monitoring directory: /usr/share/applications/

File Created: gedit.desktop
App install/uninstall detected

Generating software.list
send event

Monitoring directory: /usr/share/applications/

File Deleted: vlc.desktop
App install/uninstall detected

Generating software.list
send event

Monitoring directory: /usr/share/applications/

File Created: vlc.desktop
App install/uninstall detected

Generating software.list
send event


15 ] 
	sudo sed -i 's/port=3389/port=33220/g' /etc/xrdp/xrdp.ini  --> replace port=3389 to port=33220 on cli

16] Freerdp form host 192.168.2.58 to 192.168.2.220 vm connection command

	xfreerdp /v:192.168.2.220:33220 /u:rushi /p:rushi@123 /cert:ignore /bpp:16 /gdi:sw

17] 	du -sh /media/sda4/*   --> to show total size of each directory inside /media/sda4/*
	du -sh /media/sda4/*  | sort -h  --> sort in assending order

18] 	Download escan build 

	1. 64 co-operate  -> wget http://www.microworldsystems.com/download/wmclmclient/deb/escan-antivirus.amd64.deb

	2. dlp 		  -> wget http://www.microworldsystems.com/download/wmclmclient/deb/dlp/escan-dlp.amd64.deb

19]   find / -type f -iname "mwlogdaemon.cpp" -exec ls -lrtha {} \; 2>/dev/null
-> {} acts as a placeholder for the file that find discovers. It needs a space before it so it's treated as a distinct argument by ls.

\; tells find that the -exec command has finished. It must have a space before it, otherwise find doesn't know where the execution string ends. 

13] scp file form server/host pc to cilent, form client computer w/o going to server terminal.

root@rushi-VB-220:/tmpdata# scp -P 8623 root@192.168.2.58:/tmpdata/Rushikesh_27022026/edr.sh .
root@192.168.2.58's password: 
edr.sh                                                                                   100%   19KB  12.0MB/s   00:00 

14]	grep -iErn --color "dlp|edr|xdr|ultra|corpo" .

grep

👉 search tool

-i

👉 ignore case (DLP = dlp = Dlp)

-E

👉 enable extended regex → allows | (OR)

-r

👉 recursive → search all files + subdirectories

-n

👉 show line number

--color

👉 highlight matched words

"dlp|edr|xdr|ultra|corpo"

👉 search any of these words (OR condition)

.

👉 current directory

Search ALL files in current directory and subdirectories
→ find lines containing:
dlp OR edr OR xdr OR ultra OR corpo

15]	xdg-open command.odt & xdg-open program.odt &	

16]
	gnome-terminal -- bash -c "sudo -S bash -c 'cd /tmpdata/Rushikesh_27022026 ; xdg-open command.odt & xdg-open program.odt; exec bash'" &

17]	dpkg-query --search /usr/share/applications/com.google.Chrome.desktop
google-chrome-stable: /usr/share/applications/com.google.Chrome.deskto

18]	
	dpkg -P escan-dlp
	rm -rf /opt/MicroWorld/ /var/MicroWorld/ /etc/escandet.dat /etc/microworld.keys /etc/.p*.e*

	/opt/MicroWorld/usr/bin/updatenow_avs --force
	rm /opt/MicroWorld/var/backavsupd/dirlist.txt /opt/MicroWorld/var/avsupd/dirlist.txt /var/MicroWorld/var/updinfo/*


19] 	grep -lE "Exec=.*(eog|evince)" /usr/share/applications/*.desktop
	
	grep -lE "Exec=.*(eog|evince)" /usr/share/applications/*.desktop

    -l: Only shows the filenames, not the matching lines.

    Exec=.*: Specifically looks for the line that tells Linux how to "Execute" the app.	

20]  vim slipt screen 
	Horizontal 	-> vim -o a1.cpp b2.cpp -> 'o' small o
	vertical	-> vim -O a1.cpp b2.cpp -> 'O' capital 

21]  vim paste one-file to another-file 
	vim -O a1.cpp b2.cpp -> file open vertically -> v 'visual mode' -> select -> yy -> paste -> ctrl + w + w -> switch tab / file -> go to line where to paste ':100' -> p -> paste -> w: -> save.

22] klavaro --> type master comman

23] gnome-terminal -- bash -c "sudo -S bash -c 'cd /tmpdata/Rushikesh_27022026 ; xdg-open command.odt & xdg-open program.odt; exec bash'" &

   gnome-terminal --working-directory=/tmpdata/Rushikesh_27022026 -- bash -c "xdg-open command.odt && xdg-open program.odt; exec bash"

   gnome-terminal -- bash -c "cd /tmpdata/Rushikesh_27022026 && sudo xdg-open command.odt & sudo xdg-open program.odt; exec bash"

   gnome-terminal -- bash -c "cd /tmpdata/Rushikesh_27022026 && xdg-open program.odt && xdg-open command.odt; exec bash"	
25] find file own by user "omkar"

	sudo find / -user omkar 2>/dev/null

26] 
   {char s[5000];sprintf(s, "echo 'motherboardModel %s' >> /tmp/shubhamPC.Log", motherboardModel.c_str());system(s);}

Performance	Con	Calling system() is expensive. It has to start a whole new shell process just to write one line.
Security	Con	Command Injection Risk. If motherboardModel contains a semicolon (e.g., "; rm -rf /"), the system will execute that command too.
Buffer Safety	Con	sprintf doesn't check limits. If the model name is somehow huge, it could cause a Buffer Overflow.

27]

	If you have a...				Use this Tool
	Segmentation Fault (Crash)			GDB (to find the line that crashed)
	Memory Leak / Slow RAM usage			Valgrind
	Permission / File Not Found error		strace
	General Logic / Flow check			Your XXPRINT macro 

28]  command history with time and date

	vim ~/.bashrc
	paste -> export HISTTIMEFORMAT="%d/%m/%y %H:%M:%S -> "  #date time -> commandHistory
	close -> :wq
	source ~/.bashrc
	histor

29] Vim maintains a "Jumplist" of locations you have visited.

    Ctrl + o: Moves backward through your jump history (Go to "Older" location).

    Ctrl + i: Moves forward through your jump history (Go to "Inward/Newer" location).

30]  Open with LibreOffice Calc:
bash

libreoffice --calc interview_roadmap.csv

When the import dialog appears, choose:

    Separator options: Comma (,)

    Character set: Unicode (UTF-8)

    First line as header: Yes

31] Copy all the text above.

Open terminal and run:
bash

cat > interview_roadmap.csv

then paste the content and press Ctrl+D.

32] # Correct syntax: unzip <archive> -d <destination>
		unzip MyWorkDcoumentations.zip -d documentation/

33] # Correct syntax: zip -r <name_of_zip> <folder_to_zip>
		 zip -r MyWorkBackup.zip documentation/
